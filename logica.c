#include "logica.h"
#include <stdlib.h>

// Implementar isso de verdade depois
bool avaliar(Proposicao* prop) {
    if (prop == NULL) return false;
    
    // (LÓGICA RECURSIVA ENTRARÁ AQUI)
    
    return false; 
}

// (Faltam as implementações de criarAtomo, criarOperador, destruirProposicao)